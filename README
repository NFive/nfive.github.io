# NFive.github.io

Website source files for NFive.

## Development
Development will require node package manager (npm) to be installed on your system.

1. Clone this repository locally:
	```sh
	git clone https://github.com/NFive/NFive.git nfive.github.io
	cd nfive.github.io
	```

2. Install node packages:
	```sh
	npm install
	```

3. Make changes and build during development automatically
	```sh
	npm run watch
	```

4. Build production files
	```sh
	npm run build
	```
